<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.sist.company.CompanyMapper">
    <select id="selectCompanyData" resultType="companyDomain">
     select company_name as companyName, company_tel as companyTel from COMPANY
     where company_no=#{companyNo}
    </select>
    
    <update id="updateCompanyData" parameterType="companyDTO">
     update COMPANY
     set company_name = #{cDTO.companyName}, company_tel = #{cDTO.companyTel}
     where company_no = #{companyNo}
	</update>
    
</mapper>