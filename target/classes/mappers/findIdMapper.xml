<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="kr.co.sist.findId.FindIdMapper">
  
  <select id="selectFindId" resultType="String" >
  SELECT USER_ID FROM users WHERE USER_NAME=#{name, typeHandler=kr.co.sist.login.EncryptTypeHandler AND EMAIL=#{email, typeHandler=kr.co.sist.login.EncryptTypeHandler}
  </select>
  
  </mapper>