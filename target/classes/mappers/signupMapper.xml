<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.sist.signup.SignupMapper">
    <select id="selectIdDup" parameterType="string" resultType="int" >
        SELECT COUNT(*) FROM users WHERE user_id = #{user_id}
    </select>
    
    <insert id="insertUserJoin" parameterType="kr.co.sist.signup.UserDTO" >
        INSERT INTO users(user_name, user_id, password, email, phoneNumber,ip, signup_Date,ACCOUNT_STATUS) 
        VALUES (#{name, typeHandler=kr.co.sist.login.EncryptTypeHandler}, #{userId}, #{password}, #{email, typeHandler=kr.co.sist.login.EncryptTypeHandler}, #{tel, typeHandler=kr.co.sist.login.EncryptTypeHandler},#{ip} ,SYSDATE,'활성')
        <selectKey keyProperty="userNo" resultType="string" order="AFTER">
        SELECT MAX(user_no) FROM users
    </selectKey>
    </insert>
    
    <insert id="insertUserRole" parameterType="kr.co.sist.signup.UserDTO" >
        INSERT INTO role(user_no, role_name,role_level ) 
        VALUES (#{userNo, jdbcType=VARCHAR},'일반사용자',1 )
    </insert>
    
    <insert id="insertUserChat" parameterType="kr.co.sist.signup.UserDTO" >
        INSERT INTO CHATROOM(chatroom_name, input_date, user_no ) 
        VALUES ('나와의 채팅',sysdate,#{userNo, jdbcType=VARCHAR})
    </insert>
    
    
	<insert id="insertCompanyInfo" parameterType="kr.co.sist.signup.UserDTO">
    INSERT INTO company (company_name,company_tel)
    VALUES (null,null)
    <selectKey keyProperty="companyNo" resultType="string" order="AFTER">
        SELECT MAX(company_no) FROM company
    </selectKey>
</insert>

    <insert id="insertRank1" parameterType="String">
    insert into rank(rank_name, priority, company_no) values('임원',1,#{companyNo})
    </insert>
    <insert id="insertRank2" parameterType="String">
    insert into rank(rank_name, priority, company_no) values('관리직',2,#{companyNo})
    </insert>
    <insert id="insertRank3" parameterType="String">
    insert into rank(rank_name, priority, company_no) values('일반직',2,#{companyNo})
    </insert>
    <insert id="insertRank4" parameterType="String">
    insert into rank(rank_name, priority, company_no) values('아르바이트',2,#{companyNo})
    </insert>
    
    <insert id="insertPosition1" parameterType="String">
    insert into position(position_name, priority, company_no) values('사장',1,#{companyNo})
    </insert>
    <insert id="insertPosition2" parameterType="String">
    insert into position(position_name, priority, company_no) values('부장',2,#{companyNo})
    </insert>
    <insert id="insertPosition3" parameterType="String">
    insert into position(position_name, priority, company_no) values('대리',3,#{companyNo})
    </insert>
    <insert id="insertPosition4" parameterType="String">
    insert into position(position_name, priority, company_no) values('사원',4,#{companyNo})
    </insert>
    
    
    <insert id="insertManagerJoin" parameterType="kr.co.sist.signup.UserDTO">
    INSERT INTO users(user_name, user_id, password, email, phoneNumber,ip, company_no, signup_Date,ACCOUNT_STATUS) 
    VALUES (#{name, typeHandler=kr.co.sist.login.EncryptTypeHandler}, #{userId}, #{password}, #{email, typeHandler=kr.co.sist.login.EncryptTypeHandler}, #{tel, typeHandler=kr.co.sist.login.EncryptTypeHandler}, #{ip}, #{companyNo, jdbcType=VARCHAR}, SYSDATE,'활성')
    
    <selectKey keyProperty="userNo" resultType="string" order="AFTER">
        SELECT MAX(user_no) FROM users
    </selectKey>
</insert>
    
    <insert id="insertRole" parameterType="kr.co.sist.signup.UserDTO" >
        INSERT INTO role(user_no, role_name,role_level, company_no) 
        VALUES (#{userNo, jdbcType=VARCHAR},'최고운영자',99 ,#{companyNo, jdbcType=VARCHAR})
    </insert>
    <insert id="insertManagerChat" parameterType="kr.co.sist.signup.UserDTO" >
        INSERT INTO CHATROOM(chatroom_name, input_date, user_no ) 
        VALUES ('나와의 채팅',sysdate,#{userNo, jdbcType=VARCHAR})
    </insert>
</mapper>